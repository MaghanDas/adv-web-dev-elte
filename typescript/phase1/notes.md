
what is typescript ? 
javascript + types + compiles step.
it doesn't run in the browser directly.

ex:
let name: string = "john";
typescript compiles to : 
let name = "john"
types are removed  - they exist only during development.

typescript catches bug before runtime.
